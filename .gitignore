# Terraform internal state (NEVER push)
.terraform/

# State files (NEVER push)
*.tfstate
*.tfstate.*

# Variable files (may contain secrets)
terraform.tfvars
terraform.tfvars.json

# OS files
.DS_Store
Thumbs.db